import "@testing-library/jest-dom"; // Para extender las expectativas de Jest DOM
// import { vi } from "vitest";

// // Configura cualquier mock global aquí si es necesario
// globalThis.document = document;
// globalThis.window = window;
// globalThis.navigator = navigator;
